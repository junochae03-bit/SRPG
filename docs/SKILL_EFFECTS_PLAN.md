# 다양한 스킬 이펙트 구현

대상: 최신 V0.2 작업본 `publish/SRPG/game`. RPG2는 이전 복사본으로 확인되어 구현 전에 대상을 정정했다. 다른 진행 중인 V0.2 변경과 기존 사용자 저장, 피해량·비용·쿨다운은 보존한다.

1. 완료 — `skill_vfx.gd` 18계열 렌더러와 기존 `skill_effects.gd` 통합. 전용 검사 첫 단계 Godot import PASS. 사람 검토 대기 없음.
2. 완료 — 기본/전직 스킬 모드·식별자·랭크·실제 좌표·추적·투사체 연결. `python tools/check_skill_effects.py` 최종 PASS: 448회 발동/13,173항목 실패 0, 경고 없음. 사람 검토 대기 없음.
3. 완료 — 갤러리 3장 및 실제 전투 8종 캡처, 65항목 실패 0/렌더 오류 0. 효과의 모양·잔광·범위·18종 동시 재생과 전투 화면을 육안 검토했다. 이동 56회 좌표 검증, 차지 취소는 최신 시뮬레이션 상태를 읽도록 리뷰 후 보정. 사람 검토 대기 없음.
4. 완료 — 최종 시각 보정 후 `python tools/verify_v01.py` 재실행 → `V01_GATE PASS checks=24486` (runtime/v01-checks/20260909-184910). 실행 안내와 `TEAM_HANDOFF_VFX.md`에 파일·이벤트 계약·미리보기·최종 결과 기록 완료. 사람 검토 대기 없음.

검증 단계는 독립적인 전용 검사/전체 회귀로 병렬 진행했다. 계획 수정: 갤러리 외에도 main.tscn의 실제 전투·투사체 경로 8종을 캡처해 연결 상태를 확인한다.

검증 도구는 Godot 4.6과 격리된 검사 저장을 사용한다. 효과는 제한된 수의 도형으로 그려 별도 입자 노드가 쌓이지 않게 한다.
