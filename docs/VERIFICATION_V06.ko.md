# V0.6 배포 검증

2026-09-13 · Windows 64비트 · Godot 4.6 stable.

## 소스와 DB

- `tools/verify_v01.py`: **116그룹·490,283항목 PASS**, 범위 `complete`.
- 실행 기록: `runtime/v01-checks/20260913-004415`, 완료 00:53:43. 이전 중단/부분 실행의 합산이 아닌 새 전체 실행이다.
- 런타임 361개 파일·463.73 MiB의 SHA를 검사·내보내기 전후 대조했다. 모델, 실제 UI 입력, GPU 렌더, 이전 저장의 복사본 이행·재시작, 글자/아이콘/장비/타일, DB 캐시, 설치 도구 검사를 포함한다.
- `tools/build_database.py --check`: PASS. DB 관리팀의 최종 원본 330개·원본 함수 63개·33개 테이블 인수 결과를 유지한다.
- 실제 6개 ENet 프로세스의 접속·초과 접속 거절·개인 물약 거래·사용 직후 퇴장 저장: `runtime/coop-check-1789225719559996200`, PASS.
- 실제 WASAPI 출력 경로: `audio_shutdown_v04` 51항목 PASS. 전체 소스 게이트의 headless 오디오 검사는 Dummy 드라이버이며 두 실행을 구분한다.

## Windows 실행 파일

- `tools/build_v01.py --version V0.6`: PASS.
- `tools/test_export_v01.py --version V0.6`: PASS. 기록은 `runtime/export-checks/20260913-005520`, 보고서는 `artifacts/export-check-v06.json`이다.
- 40초 자동 이동·전투에서 6회 처치·46.36타일 이동. 실행 파일 옆의 격리된 저장 슬롯을 재시작해 모든 영속 필드 일치를 확인했다.
- 시작/캐릭터 생성/키 변경/대화/가방/스킬/도감/장비 비교/수련장/코스튬 상점을 실제 EXE로 실행했다. 시작 화면의 V0.6 표기를 확인했다.
- 전직 15종, 코스튬 30종, NPC 코스튬 3종, 생태권 10종, 100층 아스트라를 실제 PCK에서 읽고 화면으로 렌더했다.
- 100층 보스 체력 400,478, 무력화 요구량 650, 체크 요구량 85 확인. 100층 완료 저장의 재시작도 확인했다. 이는 사람이 100층 전체를 플레이했다는 의미가 아니다.
- 실제 1920×1080 캡처의 빈 프레임 및 크로마 단색 블록 검사 통과. 미연결 V0.6 원화와 미사용 원본 텍스처의 패키지 제외를 실행 파일 내부에서 확인했다.

## 배포 파일

| 파일 | 바이트 | SHA256 |
|---|---:|---|
| StelRPG-V0.6-Windows.zip | 324622286 | fa3de7efb5f6d3b3bb65b9ffdbf7d28121990ac0483d578e01a987e2515a538b |
| StelRPG-V0.6-Database.zip | 2455309 | 314970cfefe5b72f175b032f95581c423b3a7d0c8d5eb76c40e9befbf0e5c6d6 |
| StelRPG.exe | 104578048 | f9cb815a32a1ae3a73ad8699ed2b068cb76fe01636d7e8538243056fac05f37b |
| StelRPG.pck | 292980184 | 0e685f94c38dcb5a6c58d63f523c3e2b79a533ab8d7d5265b7d4610f6b992f50 |

Windows ZIP은 EXE/PCK/시작 안내/출처와 라이선스 6개 파일만 포함한다. DB ZIP은 SQLite·JSON·스키마·쿼리·manifest·안내 12개 파일을 포함한다. ZIP 무결성 및 manifest 해시를 확인했으며 개인 저장·검사 산출물은 포함하지 않는다.

외부 네트워크의 장시간 협동, 직업별 장기 밸런스와 다인 전투의 주관적 음향·시각 인수는 남아 있다. 전체 벤치마킹 후속 기능이나 미연결 V0.6 제작 원본의 완료로 집계하지 않는다.
